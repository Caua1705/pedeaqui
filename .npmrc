# Faz o npm RECUSAR rodar quando a versao nao bate com "engines" do package.json,
# em vez de so avisar. Sem isto, um npm de major diferente resolve peerDependencies
# opcionais de outro jeito e grava um lock que o `npm ci` do CI rejeita com EUSAGE.
engine-strict=true
