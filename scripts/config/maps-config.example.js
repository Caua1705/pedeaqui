// Google Maps API key — TEMPLATE (safe to commit, contains no credential).
//
// Setup:
//   1. Copy this file to `maps-config.local.js` in the same folder.
//   2. Paste your key into RAPIDEX_MAPS_KEY below.
//   3. `maps-config.local.js` is in .gitignore — never commit a real key.
//
// The key must be restricted in the Google Cloud Console:
//   - Application restriction: HTTP referrers (your domains only)
//   - API restriction: Maps JavaScript API + Places API
window.RAPIDEX_MAPS_KEY = '';
